# Interview service dependencies.
# Step 2 (this scaffold) needs only the web server. LangChain / LangGraph /
# ElevenLabs clients get added in later steps as we build the agent.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
